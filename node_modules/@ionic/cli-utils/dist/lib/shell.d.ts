import { IShell, IShellRunOptions } from '../definitions';
export declare const ERROR_SHELL_COMMAND_NOT_FOUND = "SHELL_COMMAND_NOT_FOUND";
export declare class Shell implements IShell {
    run(command: string, args: string[], {showExecution, showError, fatalOnNotFound, fatalOnError, truncateErrorOutput, ...crossSpawnOptions}: IShellRunOptions): Promise<string>;
}
